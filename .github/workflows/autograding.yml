name: PL-SQL Sum Autograding

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Make test.sh executable
        run: |
          chmod +x test.sh

      - name: Run Autograding
        run: |
          ./test.sh
